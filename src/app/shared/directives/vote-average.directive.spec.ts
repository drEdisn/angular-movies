import { VoteAverageDirective } from './vote-average.directive';

describe('VoteAverageDirective', () => {
  it('should create an instance', () => {
    const directive = new VoteAverageDirective();
    expect(directive).toBeTruthy();
  });
});
